# Chargé uniquement par `vite build --mode offline`. Active le mode hors ligne
# côté client (cf. src/logic/offline.js et main.jsx). N'affecte JAMAIS le build
# en ligne (mode production).
VITE_OFFLINE=1
