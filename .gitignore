node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example
!.env.offline
.claude/
_bmad/
art/

# Artefacts transitoires de la traduction des questions (la base = source de vérité)
scripts/generated/to-translate.json
scripts/generated/translated.json

# Sauvegardes DB locales (restauration)
backups/

# Captures temporaires de design (DesignSync / tests visuels)
.tmp-*

# Fichiers de travail design des cartes (blockout, détourages sources, captures
# de contrôle) — lourds (~340 Mo) et non utilisés à l'exécution. Restent en local.
map-design/

# Sprites de brouillon (essais intermédiaires _event_*, _chest_* …) — ~14 Mo,
# non utilisés à l'exécution. Restent en local.
tmp_sprites/
